# Mock backend — naming rule

## Naming
- File name == primary public type name (exact casing).

## Granularity guidance (skill instruction)
- When the same rule is violated across multiple files **with the same mechanical fix**, group them into one item with all files in scope.
- When the same rule is violated but each violation requires a **different fix**, create one item per file.
